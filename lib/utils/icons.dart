import 'package:flutter/material.dart';

const Icon iconEmailField = Icon(
  Icons.email,
  color: Colors.green,
);
const Icon iconPasswordField = Icon(
    Icons.key,
    color: Colors.green,
);
const Icon iconNameField = Icon(
  Icons.man,
  color: Colors.green,
);